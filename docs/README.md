# Avance 2

API FastAPI desplegada con Redis, RDS PostgreSQL y AWS S3 en EC2.

## Instrucciones
1. docker build -t red-social-reto-api ./app
2. docker compose up -d
3. curl -I http://localhost:8000/docs
